#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <mpi.h>
#include <time.h>
#include <unistd.h>


int main(int argc, char *argv[]){

    // printf("Number of arguments: %d\n", argc);

    // // argv 是一个指向字符串数组的指针，每个元素是一个命令行参数的字符串
    // // argv[0] 通常是程序的名称
    // printf("Program name: %s\n", argv[0]);

    // // 打印其他命令行参数
    // for (int i = 1; i < argc; i++) {
    //     printf("Argument %d: %s\n", i, argv[i]);
    // }
    if (argc == 3) {
        printf("hh");
    return 0;
}